<div id="catshowpage">
<div class="cattitle"><%= @title %></div>

<% @array_of_events.each do |event| %>
<div id="eventdetails"><a href="/events/<%= event.id %>">
	<% if event.photo.file? %>
	<%= image_tag event.photo.url(:small) %>
	<% else %>
	<%= image_tag ("questionmark.png"), :height => 150, :width => 120 %>
	<% end %>
</a>

<p id="eventwords">
	<%= event.name %><br />
	On <%= @months[event.date.month - 1] %> <%= event.date.day %>, <%= event.date.year %><br />
	Description: <%= event.description %> <br />
	Link to Facebook Event:  <%= event.facebooklink %>	<br />
</p>
</div>


<% end %>
</div>

<div class="comment">
   <span class="dateandoptions">
   	<% if(comment.created_at)%>
   	<div id="timestamp">Posted <%= time_ago_in_words(comment.created_at) %> ago</div>
   </span>
   		<p><b><%= comment.author %></b> wrote: </p>
   		<p><%= comment.content %></p>
   	<% end %>
</div>
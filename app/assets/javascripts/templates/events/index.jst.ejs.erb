<div class="page-header">
  <h1>
    <%% if (EventSalsa.currentUser) { %>
      What's happening?
      <!-- What's happening, <%%= EventSalsa.currentUser.name %>? -->
    <%% } else { %>
      What's happening?
      <a href="/auth/facebook">
        <%= image_tag "fb-connect-large.png" %>
      </a>
    <%% } %>
  </h1>
</div>


<ul class="nav nav-tabs">
  <li>
    <a data-toggle="tab" data-target="#upcoming-events">Upcoming Events</a>
  </li>
  <li>
    <a data-toggle="tab" data-target="#recent-events">Recent Events</a>
  </li>
  <li>
    <a data-toggle="tab" data-target="#new-event">New Event</a>
  </li>
</ul>

<div class="tab-content">
  <table id="upcoming-events" class="table tab-pane fade">
  </table>

  <table id="recent-events" class="table tab-pane fade">
  </table>

  <div id="new-event" class="tab-pane fade">
  </div>
</div>
